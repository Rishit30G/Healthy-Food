import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.pexels.com", "www.freeiconspng.com", "www.freeiconspng.com", "i.postimg.cc"],
  },
};

export default nextConfig;
